# Web framework & server
fastapi>=0.115
uvicorn[standard]>=0.30

# Settings & validation
pydantic>=2.8
pydantic-settings>=2.4

# LLM providers
anthropic>=0.40
openai>=1.50

# HTTP client — used by the ElevenLabs TTS service (and by the test client)
httpx>=0.27

# Testing
pytest>=8.2

# Dev tooling — linting & import order (config in ruff.toml at the repo root)
ruff>=0.6
